# 应用目录

此目录预留给 Flask 应用，目前没有可运行代码。

- `templates/`：HTML 页面，后续实现提问输入与结果展示。
- `static/`：页面使用的 CSS 和 JavaScript。
- 后续在本目录添加 Python 应用入口、数据库查询及模型调用代码，按实际需要拆分文件。

前端通过同源接口访问后端。数据库读取、资料数量计算和未来的大模型 API 调用均由后端负责，前端不保存密钥或直接连接数据库。

当前 `.gitkeep` 仅用于让 Git 保留空目录，添加实际页面和静态文件后可移除。
